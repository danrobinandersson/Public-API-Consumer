# Public-API-Consumer

Small project that aims at fetching data from a public API

# To run locally:

An API key is needed to run the project locally.
To retrieve a key, create a free account at https://www.weatherapi.com/ and add the key to a config.js file in this way:
`export const WEATHER_API_KEY = "your_api_key_here"`
